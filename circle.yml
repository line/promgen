machine:
  python:
    version: 3.5.2
  environment:
    SECRET_KEY: abcd
dependencies:
  override:
    - pip install -e .[dev]
test:
  override:
    - promgen test
  post:
    - mkdir -p $CIRCLE_ARTIFACTS/cover
    - cp dist/cover/* $CIRCLE_ARTIFACTS/cover/
